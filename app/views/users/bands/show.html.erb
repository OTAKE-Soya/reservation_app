<div class="container">
  <div class="row">
    <h2><%= @band.name %>のメンバー</h2>
  </div>
  <div class="row">
    <ul>
      <% @band.users.each do |user| %>
        <li><%= user.name %></li>
      <% end %>
    </ul>

  </div>
</div>

